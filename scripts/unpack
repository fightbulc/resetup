#!/usr/bin/env bash

set -e  # Exit on any error

# Decrypt the data first
if ! scripts/dec; then
    echo "❌ Unpack failed - cannot proceed without valid decryption"
    exit 1
fi

echo ""
echo "🚀 INSTALLING RECIPES..."
echo ""

# Only proceed with recipes if decryption was successful
scripts/recipes
